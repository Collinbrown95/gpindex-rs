[package]
name = "python-bindings"
version = "0.1.0"
edition = "2021"

[lib]
name = "pygpindex"
crate-type = ["cdylib"]

[dependencies]
core-logic = { path = "../core" }
pyo3 = { version = "0.20", features = ["extension-module"] }
numpy = "0.20"
